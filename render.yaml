services:
  - type: web
    name: crypto-trading-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: "python cron_job.py & uvicorn main:app --host 0.0.0.0 --port $PORT"
